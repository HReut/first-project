# first-project

A boilerplate for a single-page application (SPA), built with [Vite](https://vitejs.dev/) and TypeScript.

## Getting started

```bash
npm install
npm run dev
```

## Scripts

- `npm run dev` — start the development server
- `npm run build` — type-check and build for production
- `npm run preview` — preview the production build locally
